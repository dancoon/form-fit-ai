# Mobile

Expo app. Commands from this directory, or via root `package.json` (`bun start`, `bun test`, …).

```bash
bun install
bun start          # Expo Go
bun run dev        # dev client — camera, TFLite, MediaPipe
```

[../docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md) · [src/lib/squat/README.md](src/lib/squat/README.md)
