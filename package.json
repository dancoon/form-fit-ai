{
  "name": "form-fit-ai",
  "private": true,
  "scripts": {
    "start": "bun run --cwd mobile start",
    "dev": "bun run --cwd mobile dev",
    "android": "bun run --cwd mobile android",
    "ios": "bun run --cwd mobile ios",
    "web": "bun run --cwd mobile web",
    "build": "bun run --cwd mobile build",
    "check": "bun run --cwd mobile check",
    "clean": "bun run --cwd mobile clean",
    "test": "bun run --cwd mobile test",
    "test:squat": "bun run --cwd mobile test:squat",
    "typecheck": "bun run --cwd mobile typecheck",
    "lint": "bun run --cwd mobile lint",
    "doctor": "bun run --cwd mobile doctor",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^2.1.6"
  }
}
